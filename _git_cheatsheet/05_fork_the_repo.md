## Шаг 1. Форк репозитория
1. Зайди на страницу нужного репозитория на GitHub.  
2. В правом верхнем углу нажми кнопку **Fork**.  
3. Выбери свой аккаунт или организацию, куда будет создан форк.  

## 🔹 Шаг 2. Клонирование форка на локальную машину
```bash
git clone https://github.com/<твоя-учётка>/<имя-репозитория>.git
cd <имя-репозитория>
```

## Шаг 3. Добавление ссылки на оригинальный репозиторий
Чтобы отслеживать изменения в исходном проекте, нужно добавить **upstream remote**:

```bash
git remote add upstream https://github.com/<оригинал>/<имя-репозитория>.git
```

Проверить список remotes:
```bash
git remote -v
```
Ты увидишь два источника:
- `origin` → твой форк  
- `upstream` → оригинальный репозиторий  

## Шаг 4. Получение изменений из оригинала
Когда в оригинальном репозитории появляются новые коммиты, подтяни их так:

```bash
git fetch upstream
git checkout main
git merge upstream/main
```

или более современный вариант (чистая история без merge-коммитов):

```bash
git fetch upstream
git rebase upstream/main
```
## Шаг 5. Отправка обновлений в свой форк
После синхронизации с оригиналом можно обновить свой форк:

```bash
git push origin main
``` 